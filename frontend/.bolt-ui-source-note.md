# Matchday Studio UI source of truth

The visual presentation in `frontend/src/App.tsx` and `frontend/src/index.css` is the recovered Matchday Studio/Bolt-derived build used for the current product.

Do not replace it with older MatchLab, Render, Streamlit, or unrelated GitHub UI code. Backend/data work should be wired underneath this presentation layer.
